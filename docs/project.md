# Project: RoadWolf

RoadWolf is a modern coach booking application built with React, Vite, and TypeScript. It allows users to search for bus routes, select seats, and book tickets.

## Tech Stack

- **Framework:** React 18
- **Build Tool:** Vite
- **Language:** TypeScript
- **Styling:** 
  - Tailwind CSS (v4)
  - Emotion (Styled Components)
  - Shadcn UI / Radix UI (Primitives)
  - Material UI (Icons/Components)
- **Routing:** React Router DOM
- **Icons:** Lucide React, MUI Icons

## Key Features

- **Home Page:** Search interface for bus routes.
- **Booking Flow:**
  - Route Selection
  - Seat Selection (`SeatSelectionPage`)
  - Passenger Details (`BookingPage` steps)
  - Ticket Confirmation (`CheckTicketPage`)
- **UI Components:** Comprehensive set of accessible components based on Radix UI and Shadcn UI (in `src/app/components/ui`).

## Project Status

The project has reached a **Polished MVP** stage. The core booking flows (Search → Seat Selection → Passenger Info → Payment) are fully implemented and optimized for **Responsive Design**, offering a native-app-like experience on mobile devices and a rich interface on desktop.
